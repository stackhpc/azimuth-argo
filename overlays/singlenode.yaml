# On a single node, we have to use a single Consul replica
consul:
  release:
    values:
      server:
        replicas: 1

# Use port 2222 for Zenith SSHD
zenith:
  release:
    values:
      sshd:
        service:
          port: 2222
